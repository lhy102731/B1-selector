BLOCKER: nonce_hmac_domain_sep: HMAC key is root_secret but no explicit domain-separation constant (e.g., “nonce-fp-v1”) in the HMAC input; a future reuse of the same key+nonce shape for another purpose could produce identical fingerprints.
MUST_FIX: final_eval_cas_race: begin step updates ticket ISSUED->IN_PROGRESS and binding AUTHORIZED->CONSUMED in “one writer transaction,” but if the transaction aborts after ticket CAS but before binding CAS, the ticket is IN_PROGRESS with no binding; recovery must handle this orphan state.
MUST_FIX: final_eval_claim_double_bind: UNIQUE result_claim_ref prevents double-binding a claim to one ticket, but nothing prevents two different tickets from binding the same result_claim_ref if the claim is created once globally and referenced by both.
MUST_FIX: recovery_reopen_holdout: FinalEvalRecoveryLease “can only close/terminal/outbox, never reopens or reissues” but does not explicitly forbid advancing saga_state from RESULT_STAGED back to EVALUATING after crash before staging; need epoch check on resume.
MUST_FIX: wal_require_wal_per_conn: verification of journal_mode=WAL “before each BEGIN” is check-only; a concurrent connection may alter journal_mode after the check and before the transaction starts (TOCTOU).
MUST_FIX: migration_anchor_atomicity: build the canonical row-hash prefix chain over historical rows inside one BEGIN IMMEDIATE, but if the chain rebuild crashes mid-transaction, the rollback leaves original rows intact but the integrity root absent; verifier must distinguish “no root” from “root mismatch” or fail-open.
MUST_FIX: migration_quiescence_stale: read-only quiescence checks IN_PROGRESS tickets/outbox/WAL at one instant; a ticket may flip IN_PROGRESS between the check and the SQLite-backup snapshot, capturing an inconsistent event set.
MUST_FIX: coordinator_replay_tampered_blob: envelope manifest is read from committed blob, but envelope content (the actual patch/instruction payload) is validated only by SHA-256 of the manifest, not the payload bytes; a tampered blob with a recomputed manifest hash passes if attacker can write the manifest blob.
MUST_FIX: git_evidence_traversal_race: reader validates path/no-NUL and mode before reading, but between validation and read, a malicious process could replace the file with a symlink (TOCTOU); need openat2/O_NOFOLLOW.
MUST_FIX: coordinator_crash_boundary: pre-BEGIN crash verified to leave zero Authority/branch change, but post-VERIFY/pre-RECEIPTS crash leaves VERIFY state with no receipt; resume must not skip to FINISH without re-running verification.
MUST_FIX: derived_tables_authorization_opaque: “no ticket/secret/lease/grant columns” is passive; derived tables could still be used as a side-channel if their content leaks ticket_id hashes or nonce fingerprints through projection values.
MUST_FIX: migration_publish_rollback_unbounded: validated publish refuses active objects, but if the publish fails after partially updating operational_meta or after copying some derived-table rows, rollback relies on external backup restore, not an in-place bounded rollback.
MUST_FIX: coordinator_downgrade_guard: v1->v2 migration finishes v2 and then “only idempotent outbox mirror/ack” — but nothing prevents a future activation from running the v1 bootstrap mode after v2 events exist, allowing downgrade.
INFO: sql_injection: all queries appear parameterized; no string-concatenated SQL seen in supplied deltas.
INFO: shell_injection: git commands use explicit argv vectors, no shell=True or concatenation observed.
INFO: verifier_derived_race: verify_access_integrity recomputes chain across rows while concurrent appends may commit new anchors mid-scan; snapshot isolation needed.
SHOULD_FIX: final_eval_safe_scan_fixed_fields: “returns fixed fields only” is not enforced by a column-level view; any code path with DB access can select nonce_fingerprint (though nonce never stored, fingerprint is high-value).
SHOULD_FIX: coordinator_single_process_lease: single parent process holds one JIT lease; if that process is killed -9 and the lease is in-memory only, recovery cannot identify the lease owner across reboot without persistent marker.
SHOULD_FIX: migration_backup_manifest_hash: SQLite-backup-API snapshots with hashed manifests — but hashes are computed over output files after write; a torn write could produce valid hash of corrupt file.
VERDICT: FAIL
Rationale: Multiple MUST_FIX races (WAL TOCTOU, git_evidence symlink race, quiescence-stale), a missing HMAC domain-separation constant, and a double-claim binding loophole threaten correctness and security guarantees claimed in the P0-CR-008 spec.