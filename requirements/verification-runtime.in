# Verification runtime direct inputs.
# Resolver: pip-tools==7.6.0
# Target runtime: CPython 3.13 on Windows
# Single environment for the full repository discovery
# (`python -m unittest discover -s tests -p "test_*.py"`).
# Generated by merging requirements/control-plane.in and requirements/quant-runtime.in
# plus the actual import surface of the test suites. See the V342 corrective
# dependency change request for the httpx conflict resolution.

# Typed foundations (control-plane)
pydantic>=2.6.1,<3
PyYAML>=6.0.2,<7
python-dotenv>=1,<2
jsonschema>=4,<5
psutil>=7.2,<8

# Legacy AG2 research-runtime compatibility (control-plane)
ag2[openai]==0.13.3
openai>=2.41,<3
tenacity>=8.5,<9

# Core numerical, indicators, and cache formats (quant-runtime)
numpy>=2,<3
pandas>=2.2,<3
pandas-ta==0.4.71b0
scipy>=1.14,<2
fastdtw==0.3.4
pyarrow>=18,<24
duckdb>=1.5,<2

# Configuration, network access, and progress reporting (quant-runtime)
requests>=2.32,<3
tqdm>=4.66,<5

# A-share market-data providers used by production and repair entry points
thsdk>=1.7.18,<2
akshare>=1.18,<2
baostock==0.9.1
beautifulsoup4>=4.12,<5
lxml>=5,<7
py-mini-racer==0.6.0

# Brick production scoring and model artifacts
joblib>=1.4,<2
scikit-learn>=1.6,<2
lightgbm>=4.5,<5

# Published Web and L2 desktop entry points
Flask>=3.1,<4
PyQt5==5.15.11
matplotlib>=3.9,<4
mootdx>=0.11.7,<0.12
tdxpy==0.2.7

# httpx conflict: control-plane requires httpx>=0.28.1 while mootdx 0.11.7
# requires httpx<0.26. Resolution is pending the dependency change request.
