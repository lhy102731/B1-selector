# P0-CR-003: Reconcile the P0R2 code/test file scope

Status: `AWAITING_OPERATOR_APPROVAL`
Prepared: 2026-07-26

## Reason

The T7 review found two integrity defects before code freeze:

1. `implementation_baseline_v342_p0r2.json` had been incorrectly treated as a rolling implementation manifest. The original byte-immutable baseline has now been restored to the exact hash and size recorded by the plan-freeze receipt: `0bf4b985c74bf6ee7e31cbfc6886bdbe08635abd0b4344331fee3fa721bb8710`, 8351 bytes.
2. Nine narrowly scoped control-plane implementation/test files were introduced after the scope manifest was frozen, but were not named in `allowed_code_and_test_files`. They cannot be silently normalized into the final P0 gate.

## Requested change

Add exactly these paths to the P0R2 code/test allowlist:

- `research_automation/control_plane/artifact_semantics.py`
- `research_automation/control_plane/cli_registry.py`
- `research_automation/control_plane/legacy_adoption.py`
- `research_automation/control_plane/provenance.py`
- `research_automation/control_plane/web_guard.py`
- `tests/test_ag2_cli_routing.py`
- `tests/test_cli_entry_guards.py`
- `tests/test_control_plane_artifact_semantics.py`
- `tests/test_control_plane_legacy_adoption.py`

No additional write root, side effect, phase, external service, data access, KBase access, research execution, Campaign authority, FinalEval authority, scheduler mutation, Git push, or auto-advance permission is requested.

## Integrity handling

- The original scope manifest, identity bundle, authorization receipt, and plan-freeze receipt remain immutable historical evidence.
- Approval authorizes creation of a supplementary scope revision, identity bundle, authorization receipt, and freeze receipt that reference this change request.
- `plan_hash` and `instruction_policy_hash` remain unchanged; the revised `scope_hash` must be recomputed from the exact revised scope payload.
- Existing commits touching the nine paths are historical, pre-detection implementation evidence only. They are not retroactively declared trusted; every affected path must be independently re-reviewed and the full P0R2 test/gate sequence rerun under the revised identity.
- The implementation baseline remains the original frozen baseline. Implementation deltas belong in TaskReport V2 `changed_files` and gate file-delta evidence, never in the baseline document.

## Approval command

To approve only this bounded scope reconciliation, send exactly:

```text
APPROVE_CHANGE_REQUEST id=P0-CR-003
```

Any other instruction must be recorded verbatim and interpreted conservatively; it must not be represented as the exact command above.

## Unchanged boundary

After P0 closes, the controller must still stop with:

```text
P1_AUTHORIZED=false
CAMPAIGN_AUTHORIZED=false
FINAL_EVAL_AUTHORIZED=false
AUTO_ADVANCE=false
```
