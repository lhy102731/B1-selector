# P0-CR-004: Reconcile P0R2 scope after the operator-owned THSDK migration

Status: `AWAITING_OPERATOR_APPROVAL`
Prepared: 2026-07-28
Supersedes if approved: `P0-CR-003` (never approved)

## Operator statement and conservative interpretation

The operator stated:

```text
忘记每日数据更新这个旁支任务，继续按照原计划推进。我已经更换了新的数据库了
```

This statement:

- removes the abandoned Eastmoney/Tencent daily-update repair from the active work;
- confirms that the THSDK/同花顺 database migration is operator-owned parallel work that must be preserved;
- authorizes read-only reconciliation and preparation of this request;
- does **not** constitute the exact approval command for this request;
- does not authorize data mutation, research, backtests, Campaign, FinalEval, scheduler changes, Git push, or automatic phase advance.

The abandoned daily-update working-tree files are neither staged nor reverted by P0R2.

## Reason

The T7 review found the two issues already recorded by `P0-CR-003`:

1. The immutable P0R2 implementation baseline had temporarily been treated as a rolling manifest and was restored to its receipt-bound identity: SHA-256 `0bf4b985c74bf6ee7e31cbfc6886bdbe08635abd0b4344331fee3fa721bb8710`, 8351 bytes.
2. Nine narrowly scoped control-plane implementation/test paths were introduced after the original scope manifest was frozen.

After that request was prepared, the operator replaced the production market-data database with the THSDK/同花顺 dataset. The live `data/` tree remains an excluded data root, but the migration also created seven additional top-level data/output trees that the fail-closed T7 scanner correctly refuses to classify without an explicit scope decision:

- `data_ths/`
- `data_pre_ths_backup_20260727_110350/`
- `outputs/`
- `ths-rebuild-1s3f37j2/`
- `ths-rebuild-2f8lznck/`
- `ths-rebuild-gzsqa360/`
- `ths-rebuild-rn72aj5e/`

A read-only suffix audit on 2026-07-28 found no `.py`, `.pyw`, `.ps1`, `.bat`, `.cmd`, `.exe`, `.dll`, `.pyd`, `.so`, `.js`, `.ts`, or `.jar` files in those seven trees. Any later executable file or unknown top-level directory must still fail closed.

THSDK-related source and tests under existing bounded code roots such as `tools/`, `utils/`, and `tests/` are **not** excluded. They remain part of the T7/T8 byte freeze while remaining outside P0R2 write authority.

## Requested bounded change

### 1. Carry forward the unapproved CR-003 code/test reconciliation

Add exactly these paths to the P0R2 code/test write allowlist:

- `research_automation/control_plane/artifact_semantics.py`
- `research_automation/control_plane/cli_registry.py`
- `research_automation/control_plane/legacy_adoption.py`
- `research_automation/control_plane/provenance.py`
- `research_automation/control_plane/web_guard.py`
- `tests/test_ag2_cli_routing.py`
- `tests/test_cli_entry_guards.py`
- `tests/test_control_plane_artifact_semantics.py`
- `tests/test_control_plane_legacy_adoption.py`

### 2. Classify the new database/output trees without granting write access

Permit `research_automation/control_plane/inventory.py` and the already allowed `tests/test_control_plane_entry_guard.py` to classify only the seven exact directories listed above as non-executable data/output roots.

- P0R2 receives no permission to read their contents recursively beyond bounded type/suffix/reparse checks needed to prove they are not executable roots.
- P0R2 receives no permission to write, rename, delete, archive, rebuild, or update those directories.
- Prefix or wildcard exclusions are forbidden. A new `ths-rebuild-*` directory requires a new explicit scope decision.
- Symlinks, junctions, generic reparse points, unreadable paths, or executable suffixes fail closed.

### 3. Preserve the concurrent operator-owned source delta

Before T7 code freeze, record a supplementary, content-addressed workspace-delta manifest under `research_state/control_plane/p0r2/` that:

- preserves the original implementation baseline byte-for-byte;
- records current path, byte size, and SHA-256 for source/test files outside P0R2 write authority;
- labels those files `OPERATOR_OWNED_PARALLEL_DELTA`, not P0 implementation evidence;
- prevents P0R2 staging or modifying them;
- includes those files in the final T7/T8 executable-surface freeze;
- makes any later byte or path change return to T7.

This manifest does not validate the scientific or production correctness of the THSDK migration. It only prevents P0R2 from misattributing or overwriting it.

## Integrity handling

- `P0-CR-003` remains immutable, unapproved historical evidence and is superseded only if this request is approved.
- The original scope manifest, identity bundle, authorization receipt, plan-freeze receipt, and implementation baseline remain immutable historical evidence.
- Approval authorizes supplementary scope/identity/authorization/freeze receipts bound to this request and the exact current workspace-delta manifest.
- `plan_hash` and `instruction_policy_hash` remain unchanged; the revised `scope_hash` must be recomputed.
- Existing commits are not retroactively trusted. All affected code must be re-reviewed and the complete T7-T9 sequence rerun.
- No daily-update fallback work may be included in a P0R2 commit.

## Approval command

To approve only this bounded reconciliation, send exactly:

```text
APPROVE_CHANGE_REQUEST id=P0-CR-004
```

Any other instruction must be recorded verbatim and interpreted conservatively; it must not be represented as the exact command above.

## Unchanged boundary

After P0 closes, the controller must still stop with:

```text
P1_AUTHORIZED=false
CAMPAIGN_AUTHORIZED=false
FINAL_EVAL_AUTHORIZED=false
AUTO_ADVANCE=false
```
