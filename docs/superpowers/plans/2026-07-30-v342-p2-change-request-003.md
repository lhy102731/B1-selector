# P2-CR-003: Reconcile the T5 staging-adapter scope

Status: `AWAITING_OPERATOR_APPROVAL`

Prepared: 2026-07-30

## Reason

The pre-T6 independent review found that the frozen P2R2 phase scope omitted
two files introduced by the already completed T5 daily staging-adapter task:

- `research_automation/data_generation/daily_staging.py`
- `tests/test_daily_updater_staging.py`

The T5 task specification, ticket, TaskReport, completion receipt, code review,
and tests all name or validate these files, but a task specification cannot
expand its parent phase scope. The omission therefore blocks an honest P2
integration freeze even though the implementation itself passed review.

This request is bound to:

- current Git HEAD: `47d240ab850ef6213028c51e6a68a314c868b517`;
- P2R2 scope hash: `013ecd99bc7bdb2645573cce100956e5cc599bb2e28bfa4463f5dca27b82c6ff`;
- `research_automation/data_generation/daily_staging.py` SHA-256:
  `ab99f8c0a7f37b991c2a9057aa3c52490d13440ae364a3f0de9fd30c7ad7c9a8`;
- `tests/test_daily_updater_staging.py` SHA-256:
  `8105a7916558a5d2f8216e973d95bbb6ee0ebfef979b73c5cf9f399cbe89c5b3`.

## Requested bounded change

Add exactly the two paths above to the P2 code/test allowlist. No other code,
test, write root, side effect, runtime behavior, dependency, data source, or
phase authority is added.

Because P2R2 already has five succeeded tickets bound to the old scope hash,
the correction must use a new `p2-attempt-003` identity and grant. Rebinding
the existing attempt in place would make the final Gate internally
inconsistent.

## Minimal recovery route

1. Preserve P2R2 and its T1-T5 evidence as immutable historical evidence; do
   not rewrite or retroactively relabel it.
2. Close P2R2 fail-closed with the single reason
   `PHASE_SCOPE_OMITTED_T5_FILES`; do not roll back the reviewed Git commits.
3. Create P2R3 with the prior P2 scope plus only the two paths listed above.
4. Run one combined `P2R3-T5C-SCOPE-ADOPTION` task that hash-binds the existing
   T1-T5 reports and completion receipts, reruns the existing offline P2 test
   suites, and independently re-reviews the two added files. It must not
   reimplement T1-T5.
5. Continue with P2R3 T6 integration freeze, T7 final inventory/policy, and T8
   official Gate.

The P2 integration boundary still runs the full offline repository unittest
suite. That is test verification only; it does not authorize or run a
backtest, daily update, cache rebuild, research task, Campaign, or Holdout.

## Efficiency constraints

- Do not scan, copy, package, or hash the approximately 84GB `data/` tree.
- Do not rebuild a whole-workspace inventory or source archive.
- Use Git commit/tree/blob identity and hashes of the exact bounded artifacts.
- Do not re-run T1-T5 implementation work when the current bytes pass
  revalidation.
- Keep `CHANGELOG.md` and `docs/b1_v3_results.md` as preserved operator-owned
  tracked deltas; do not stage or modify them.

## Unchanged boundaries

This request does not authorize:

- real data or KBase content access/mutation;
- daily updater, backtest, research, parameter sweep, or cache execution;
- P3, Campaign, production promotion, or Final Holdout;
- scheduler, process, ACL, model, strategy, or production configuration change;
- Git push, merge, rebase, history rewrite, destructive cleanup, or broad
  staging;
- automatic advance beyond the P2 Gate.

## Approval command

To approve only this two-path reconciliation and the minimal recovery route,
send exactly:

```text
APPROVE_CHANGE_REQUEST id=P2-CR-003
```

Any other instruction must be recorded verbatim and interpreted
conservatively. It must not be represented as the exact command above.
