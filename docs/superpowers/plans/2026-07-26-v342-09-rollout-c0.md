# V3.4.2 Rollout C0: Offline Chaos Simulation

Status: `BASELINE_FIXED_FOR_C0`  
Plan version: `V3.4.2-P0R2`  
Attempt: `c0-attempt-001`  
Date: 2026-08-11

## Origin (read-only facts)

- Approved index `docs/superpowers/plans/2026-07-26-v342-index.md` Rollout boundary item 1: `C0: offline fake-provider, fake-clock, fake-PID chaos simulation for at least 20 cycles.`
- Complete plan draft `docs/superpowers/evidence/2026-07-26-v342-complete-plan-draft.md` section 12.2: `C0：离线 fixtures + fake clock/PID/provider，至少 20 cycle chaos simulation。`
- P6 plan gate text: `Implementation uses fake providers only. A real Campaign requires the separate C0-C3 rollout authorization in the index.`
- P8 official closure: `research_state/control_plane/p8/attempts/p8-attempt-001/evidence/official_p8_closure_receipt_v342_p8r2.json`, active policy `6e921d37d899369ca5e0e2645e496731fb6ca992effe340f370514e9095aa3da`.

C0 is the only offline rollout gate. C1 (real-LLM dry run), C2 (two-cycle canary) and C3 (bounded Campaign) each require a separate explicit operator authorization and are out of scope here.

## Scope

Deterministic offline chaos simulation of the durable P6 Campaign controller for at least 20 cycles using fake provider, fake clock, and fake process identity. No real provider, no network traffic, no real data/KBase/Authority/Operational store, no Campaign, no Final Holdout, no scheduler/ACL, no production parameter changes.

## Deliverables

1. `research_automation/control_plane/rollout_chaos.py` - deterministic offline chaos simulation driver (fake provider/clock/PID, fault injection, invariant verification, canonical report).
2. `tests/test_control_plane_rollout_chaos.py` - RED-to-GREEN test matrix for the driver.
3. `run_research.py rollout --stage c0` CLI entry - runs at least 20 cycles and writes canonical evidence under `research_state/control_plane/rollout/c0/attempts/c0-attempt-001/evidence/`.

## Acceptance matrix

| Item | Requirement |
|---|---|
| min_cycles | >= 20 completed cycles (official run targets 24) |
| offline_only | every provider is fake; no outbound network; fixture root under a temp directory |
| chaos_categories | crash_between_steps; mid_call_provider_failure (timeout retry / invalid JSON / in-doubt); lease_fencing_fail_closed; pid_reuse_fail_closed; budget_exhaustion_fail_closed; safe_boundary_pause |
| invariants | cycle_completed_exactly_once; no_duplicate_acquisition; budget_settled_exactly_once; learning_commit_exactly_once; deterministic_replay_same_seed; no_real_side_effects; campaign_completed |
| deterministic_replay | same seed produces identical scenario log and final state digest |

## Prohibited

- Real LLM/provider calls of any kind.
- Any write to the production `research_state/control_plane/authority` or `operational` stores (fixtures only, temp root).
- Final Holdout bytes, real data generation, KBase, Memory, Registry, Snapshot, Handoff, scheduler, ACL, or production strategy parameters.
- Modifying protected worktree files (`CHANGELOG.md`, `daily_run.py`, `daily_select.py`, `docs/b1_v3_results.md`).
- `git reset`, `rebase`, `push`, broad staging, rewriting history, or cleaning untracked user files.

## Pass criteria

1. Official C0 run completes >= 20 cycles with all acceptance matrix items green.
2. Validation receipt records focused test suite plus the official run digest.
3. Independent spec review and independent quality review have zero BLOCKER / MUST_FIX findings.
4. Deterministic replay reproduced the same scenario log and state digest.
5. A single C0 gate artifact is built, verified, and closed; non-blocking findings go to the post-C0 task pool only.

## Post-C0 task pool (deferred, not this attempt)

- C1 real-LLM dry run (separate operator authorization required).
- C2 two-cycle canary (separate operator authorization required).
- C3 bounded Campaign (separate operator authorization required).
- Restore volcano seats on relay quota recovery (currently 429 for glm-5.2 and doubao-seed-2.0-pro as of 2026-08-11 probe).