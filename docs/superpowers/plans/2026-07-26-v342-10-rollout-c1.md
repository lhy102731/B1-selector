# V3.4.2 Rollout C1: Real-LLM Dry Run

Status: BASELINE_FIXED_FOR_C1
Plan version: V3.4.2-P0R2
Attempt: c1-attempt-001
Date: 2026-08-11

## Origin (read-only facts)

- Approved index docs/superpowers/plans/2026-07-26-v342-index.md Rollout boundary item 2: C1 separately authorized real-LLM dry run; no scientific Learning Commit.
- Complete plan draft docs/superpowers/evidence/2026-07-26-v342-complete-plan-draft.md section 12: C1 real-LLM dry-run validates roster/usage/context/budget only; no scientific Learning Commit.
- C0 official closure: research_state/control_plane/rollout/c0/attempts/c0-attempt-001/evidence/official_c0_closure_receipt_v342.json (gate sha256 1dc352059a40428548481890ccba8105636d32530b4783a6df0c15de2b5053f5).
- Operator standing goal (2026-08-11): "我授权的是P0-P8/P9的整个大项目的权限给你" plus "如无意外则一直推进下去直到整个大计划结束". Recorded verbatim in the C1 authorization-ref receipt as the separate C1 operator authorization.

## Scope

A real-LLM dry run through the existing local relay (Volcano Coding Plan models: doubao-seed-2.0-pro, glm-5.2, kimi-k2.7-code, minimax-m3; plus DeepSeek direct) that validates model roster reachability, per-model usage metering, dry-run context assembly, and budget accounting. No scientific Learning Commit, no Learning Packet/Ledger/Memory write, no Campaign, no Final Holdout, no real data/KBase content, no production Authority/Operational write (fixture stores only), no scheduler/ACL mutation, no production strategy parameter change.

The dry run uses harmless control-plane ping prompts only (no research data, no strategy text, no KBase content). Provider failures (HTTP 429/5xx/timeout/malformed JSON) are recorded as evidence; the official run PASS requires all configured models to succeed.

## Deliverables

1. research_automation/control_plane/rollout_c1.py - real-LLM dry-run driver (roster/usage/context/budget verification, canonical report, digest, dry-run-only guard).
2. research_automation/control_plane/rollout_providers.py - relay/direct LLM client adapters with usage parsing and failure classification.
3. research_automation/control_plane/rollout_c1_usage.py - dry-run usage ledger and budget verdict.
4. research_automation/control_plane/rollout_c1_context.py - data-free dry-run context assembly and token estimation.
5. research_automation/control_plane/rollout_c1_report.py - C1 report schema, serialization, manifest-to-report mapping lint.
6. tests/test_control_plane_rollout_c1*.py - RED-to-GREEN test matrix (driver, providers, usage, context, report, integration, CLI).
7. run_research.py rollout --stage c1 CLI entry - runs the dry run and writes canonical evidence under research_state/control_plane/rollout/c1/attempts/c1-attempt-001/evidence/; refuses to overwrite an existing report without --force (C0-POOL-001).

## Acceptance matrix

| Item | Requirement |
|---|---|
| real_llm_dry_run | each configured model is called once with a harmless data-free prompt through the approved relay/direct endpoint |
| roster_verified | configured model roster equals the approved model set (DeepSeek + doubao + glm + kimi + minimax) |
| usage_verified | per-model input/output/total tokens recorded and summed from provider usage fields |
| context_verified | dry-run context is data-free, model-bound, cycle-bound and token-estimated |
| budget_verified | usage ledger settles against the dry-run budget and reports PASS/FAIL with detail |
| no_learning_commit | no LearningCommitService / Learning Packet / Ledger / Memory write occurs |
| no_real_campaign_or_holdout | no Campaign / Final Holdout / real data generation side effects |
| failures_recorded_not_hidden | provider failure statuses are recorded; report PASS only when all models succeed |
| report_canonical | canonical JSON report with stable digest; manifest-to-report mapping lint empty |
| evidence_append_only | CLI refuses to overwrite an existing C1 report without --force (C0-POOL-001) |

## Prohibited

- Scientific Learning Commit, Learning Packet/Ledger/Memory/Registry/Snapshot/Handoff writes.
- Real Campaign, Final Holdout, real data generation, backtest, KBase content writes.
- Any write to the production research_state/control_plane/authority or operational stores (fixtures only, temp root).
- Scheduler/ACL mutation, production strategy parameter changes, protected worktree files (CHANGELOG.md, daily_run.py, daily_select.py, docs/b1_v3_results.md).
- Prompts containing real research data, strategy source, model parameters, or KBase content.
- git reset, rebase, push, broad staging, rewriting history, or cleaning untracked user files.

## Pass criteria

1. Official C1 run calls all five configured models, all succeed, usage/context/budget verified, no Learning Commit, and the report digest is recorded.
2. Validation receipt records the focused test suite plus the official run digest.
3. Independent spec review and independent quality review have zero BLOCKER / MUST_FIX findings.
4. A single C1 gate artifact is built, verified, and closed; non-blocking findings go to the post-C1 task pool only.

## Post-C1 task pool (deferred, not this attempt)

- C2 two-cycle canary (separate operator authorization recorded; not started before C1 gate closes).
- C3 bounded Campaign (requires explicit AUTHORIZE_CAMPAIGN instruction).
- Full 7-seat Claude Code pool used for C1 slices; quota-recovery probes remain on a backoff loop.
